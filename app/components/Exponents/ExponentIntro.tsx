"use client";
import React from "react";
import { MathJax, MathJaxContext } from "better-react-mathjax";

const ExponentIntro = () => (
  <section className="mb-8">
    <h2 className="text-2xl font-bold mb-2">What are Exponents?</h2>
    <p>
      The exponent of a number shows how many times the number is multiplied by
      itself. For example, 2 x 2 x 2 x 2 can be written as 2 <sup>4</sup>, as 2 is multiplied by itself 4 times. 
      Here, 2 is called the 'base' and 4 is called the 'exponent' or 'power'.
    </p>
  </section>
);

export default ExponentIntro;
